const left = dx => {
  return dx === -1;
};

const up = dy => {
  return dy === -1;
};

const right = dx => {
  return dx === 1;
};

const down = dy => {
  return dy === 1;
};

export { left, up, right, down };
